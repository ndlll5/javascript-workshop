/**
 * Create an array of fruits and use forEach to print each fruit.
 */

const fruits = ['apple', 'banana', 'grape', 'mango', 'watermelon', 'strawberry', 'pomegranate'];
fruits.forEach(element => {
    console.log(element);
});
