/**
 * Create a string and use slice to extract the first 3 characters.
 */

const message = "And as he stares into the sky, there are twice as many stars as usual.";
const messageSlice = message.slice(0, 3);
console.log(messageSlice);
